{
  "name": "e2e-screenshot-tests",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server public -p 3000",
    "test": "playwright test",
    "report": "playwright show-report",
    "update-snaps": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
